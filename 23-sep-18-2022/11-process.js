// call this when you decide the running program must be terminated immediately
process.exit( 1 ); // unsuccessful exit (the number does not carry any particular significance)
process.exit( 0 ); // successful exit (pass 0)

console.log( 'end of script' );