// ? : 

const day = 'Saturday';

// condition ? value_if_condition_is_met : value_if_condition_is_not_met
const task = ( day === 'Saturday' || day === 'Sunday' ) ? 'party' : 'work';

console.log( task );