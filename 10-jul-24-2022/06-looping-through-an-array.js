const cities = [ 'New York', 'London', 'Paris' ];

for( let idx = 0; idx < cities.length; ++idx ) {
    console.log( cities[idx] );
    
    // EXERCISE: Print the number of characters in the city as well
    // Hint: cities[idx] is the string here
    console.log( cities[idx].length );
}
