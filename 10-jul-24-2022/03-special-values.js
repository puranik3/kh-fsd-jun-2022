let x; // undefined (variables not assigned a value explicitly are assigned undefined)
console.log( x );

console.log( x === undefined ); // true

console.log( null ); // we shall see where to use null later